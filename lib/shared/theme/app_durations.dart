class AppDurations {
  AppDurations._();

  static const formFocus = Duration(milliseconds: 300);
  static const navigationSelection = Duration(milliseconds: 200);
  static const segmentedControl = Duration(milliseconds: 200);
}
